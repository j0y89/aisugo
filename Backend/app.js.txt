const express = require('express');
const bodyParser = require('body-parser');

const app = express();
const port = 3000;

app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: true }));

app.post('/generate-song', (req, res) => {
    const userInput = req.body.songInput;
    const generatedSong = generateSong(userInput);
    res.json({ song: generatedSong });
});

function generateSong(input) {
    // Simula la generazione della canzone
    return "Canzone generata dall'intelligenza artificiale basata sulla creazione di testi.";
}

app.listen(port, () => {
    console.log(`Server running at http://localhost:${port}`);
});
